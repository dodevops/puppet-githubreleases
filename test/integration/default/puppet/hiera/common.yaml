classes:
    - githubreleases

githubreleases_download:
  '/tmp/release.latest.head.fromhiera.tar.gz':
    author: 'Graylog2'
    repository: 'collector-sidecar'
    use_auth: "%{facts.github_use_auth}"
    use_oauth: "%{facts.github_use_oauth}"
    username: '%{facts.github_username}'
    password: '%{facts.github_password}'
